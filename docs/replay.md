# Replay

Replay is an investigation aid, not an authorization bypass. Preserve the
source tenant and user scope, default to dry-run, and require explicit
operator approval before any side effect. Treat captured model output,
retrieved content, tool arguments, and tool results as untrusted data.

Review docs/replay-security.md and docs/incidents.md for the detailed
guardrails and incident workflow.
